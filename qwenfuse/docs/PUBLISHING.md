# 发布状态

源码、正式实验入口、路径配置、消融版本准备工具和精选结果已整理。原运行目录未修改。

已完成：工作树复制校验、风险策略源码接入、Python 语法检查、补丁格式检查、CPU 级实验规划与报告测试。发布工具统一使用 `QWENFUSE_WORKSPACE`、`QWENFUSE_MODEL` 和可选 `QWENFUSE_REPO_A/B/C/D`；不要求旧服务器的绝对目录。

尚未执行：新目录独立依赖安装、CUDA 编译、最小模型推理以及新一轮 GPU 性能实验。这些按当前要求暂缓。

历史结果 JSON 与环境冻结记录中的原路径用于溯源，不参与新实验路径配置；它们没有被改写成新实验结果。历史诊断脚本不是主入口，完整运行快照未纳入仓库。

没有初始化新 Git 仓库，也没有上传 GitHub。发布前可运行 `python qwenfuse/scripts/check_package.py`。文件校验表见 `file-sha256.json`。
